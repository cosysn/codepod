name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Sync Go workspace
        run: go work sync

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          npm ci --prefix apps/server

      - name: Run Agent tests
        run: |
          cd apps/agent
          go test ./... -v

      - name: Run Runner tests
        run: |
          cd apps/runner
          go test ./... -v

      - name: Run Server tests
        run: |
          cd apps/server
          npm test

  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Agent
        run: |
          cd apps/agent
          CGO_ENABLED=0 go build -o agent ./cmd

      - name: Build Runner
        run: |
          cd apps/runner
          CGO_ENABLED=0 go build -o runner ./cmd

      - name: Build Server
        run: |
          cd apps/server
          npm run build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: |
            apps/agent/agent
            apps/runner/runner
            apps/server/dist/
